from binarytree import Node

# Cria uma árvore manualmente
root = Node(1)
root.left = Node(2)
root.right = Node(3)

print(root)
# Saída:
# __1
# / \
# 2   3

# Cria uma árvore binária de busca aleatória
from binarytree import bst

my_bst = bst(height=3, is_perfect=False)
print(my_bst)


for(personagens > 1):
    personagens = Node(leaf_count)/2 
    cont += 1